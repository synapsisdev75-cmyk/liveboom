/**
 * Responsividad móvil + tablet (viewport < 1024px).
 * Importar estilos desde main; tokens desde componentes que midan layout.
 */
export {
  MOBILE_TABLET_MAX_PX,
  exploreLandscape,
  isMobileTabletViewport,
} from './tokens';
