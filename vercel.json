{
  "$schema": "https://openapi.vercel.sh/schema.json",
  "services": {
    "web": {
      "root": "apps/web",
      "framework": "vite",
      "installCommand": "npm install",
      "buildCommand": "npm run build",
      "outputDirectory": "dist"
    },
    "backend": {
      "root": "backend",
      "framework": "express",
      "entrypoint": "index.js",
      "installCommand": "npm install"
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": { "service": "backend" } },
    { "source": "/socket.io/:path*", "destination": { "service": "backend" } },
    { "source": "/:path*", "destination": { "service": "web" } }
  ]
}
